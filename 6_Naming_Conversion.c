#include <stdio.h>
int main()
{
  char first_name[20] = "Aadi";
  char last_name[20] = "Jain";
  int age = 44;

  printf("First name: %s, Last name: %s, Age: %d", first_name, last_name, age);
  return 0;
}
/* Create a program that declares one variable of each of the
fundamental data types (int, float, double, char) and prints their
size using sizeof() operator. */